apiVersion: v1
kind: Service
metadata:
  name: vector-search-service
  labels:
    app: vector-search-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: vector-search-service